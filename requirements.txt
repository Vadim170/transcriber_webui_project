# ---- core (always required) ----
Flask>=3.0
Werkzeug>=3.0
numpy>=1.26
sounddevice>=0.5.1
pywhispercpp>=1.4.1
webrtcvad-wheels>=2.0.14
psutil>=7.0

# ---- optional backends ----
# Install only what you need:
#
# Voxtral:
#   pip install torch transformers accelerate
#
# NVIDIA Canary / Parakeet:
#   pip install torch "nemo_toolkit[asr]>=2.2"
#
# macOS fast Parakeet CoreML:
#   requires Swift + local vendor/FluidAudio package
#
# Qwen3-ASR:
#   pip install torch "qwen-asr>=0.0.6"
#
# Quantized 4-bit / 8-bit loading for Voxtral / Qwen on CUDA:
#   pip install bitsandbytes
